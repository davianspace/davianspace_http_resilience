name: davianspace_http_resilience
description: Production-grade HTTP resilience for Dart & Flutter. Retry, circuit breaker, timeout, bulkhead, hedging, fallback, structured observability, and header-redacted logging.
version: 1.0.3
homepage: https://github.com/davianspace/davianspace_http_resilience
repository: https://github.com/davianspace/davianspace_http_resilience
issue_tracker: https://github.com/davianspace/davianspace_http_resilience/issues
license: MIT

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  davianspace_dependencyinjection: ^1.0.3
  davianspace_logging: ^1.0.3
  http: ^1.2.1

dev_dependencies:
  build_runner: ^2.4.9
  fake_async: ^1.3.1
  lints: ^6.1.0
  mockito: ^5.4.4
  test: ^1.25.2