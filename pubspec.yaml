name: davianspace_http_resilience
description: >
  A production-ready Dart/Flutter HTTP resilience package inspired by
  Microsoft.Extensions.Http and Microsoft.Extensions.Http.Resilience.
  Provides a composable middleware pipeline, Polly-style resilience policies
  (retry, circuit-breaker, timeout, bulkhead), and a typed HTTP client
  factory — all built with clean-architecture and SOLID principles.
version: 1.0.0
homepage: https://github.com/davianspace/davianspace_http_resilience
repository: https://github.com/davianspace/davianspace_http_resilience
issue_tracker: https://github.com/davianspace/davianspace_http_resilience/issues

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  http: ^1.2.1
  logging: ^1.2.0
  meta: ^1.12.0

dev_dependencies:
  build_runner: ^2.4.9
  fake_async: ^1.3.1
  lints: ^4.0.0
  mockito: ^5.4.4
  test: ^1.25.2